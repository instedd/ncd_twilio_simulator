version: "3.8"
services:
  web:
    image: crystallang/crystal:0.35.1
    command: crystal run app/twiliosim.cr
    ports:
      - 3000
    volumes:
      - ./src:/app
  ngrok:
    image: wernight/ngrok
    ports:
      - 4040
    environment:
      NGROK_PORT: 'web:3000'
