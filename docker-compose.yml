version: "3.8"
services:
  web:
    image: crystallang/crystal
    command: crystal run app/twiliosim.cr
    ports:
      - 3000
    volumes:
      - ./src:/app
  ngrok:
    image: wernight/ngrok
    ports:
      - 4040
    environment:
      NGROK_PORT: 'web:3000'
